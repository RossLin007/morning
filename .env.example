# Local env example for development
# Copy this file to .env and fill in your values

# ============================================
# Supabase Configuration
# ============================================
# Get these from your Supabase project settings:
# https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ============================================
# Google Gemini AI Configuration
# ============================================
# Get your API key from:
# https://makersuite.google.com/app/apikey
# Note: Vite requires VITE_ prefix for client-side env vars
VITE_GEMINI_API_KEY=
# VITE_API_KEY=  # Alternative naming (also supported)

# ============================================
# UniAuth SSO Configuration
# ============================================
# Get these from your UniAuth developer console
# Uses public OAuth PKCE flow (no client secret required on frontend)
VITE_UNIAUTH_BASE_URL=https://auth.55387.xyz
VITE_UNIAUTH_CLIENT_ID=

# ============================================
# Server-side JWT Verification (Required for production)
# ============================================
# Get these from your UniAuth SSO team
# Option 1: PEM format public key (replace \n with actual newlines or use \\n)
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nMIIBIjAN...\n-----END PUBLIC KEY-----"
# Option 2: HMAC shared secret
# JWT_SECRET=your-secret-key
# JWT algorithm (default: RS256)
# JWT_ALGORITHM=RS256

# ============================================
# Server Environment
# ============================================
# Allowed CORS origins (comma-separated for production)
# ALLOWED_ORIGINS=https://morning.55387.xyz,https://www.morning.55387.xyz

# ============================================
# Security Notes
# ============================================
# - NEVER commit .env file to version control
# - NEVER share your API keys
# - The VITE_GEMINI_API_KEY is used on the client side in this app
# - For production, consider proxying AI calls through a backend
# - Rotate your keys if they are ever exposed
# - JWT_PUBLIC_KEY or JWT_SECRET must be set for production deployment
